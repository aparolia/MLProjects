Name : Ayush Parolia

-- COMMAND to execute the code:
python GD.py -i <iterations> -r <regularizer> -s <step_size> -l <lambda> -f <feature_set>

Time taken : ~2-3 minutes (for Feature set 2 and 3), maximum time taken is in constructing Training, Validation and Test matrices.
